import React from 'react';
import ReactDOM from 'react-dom';
import ParentsComponent from './components/ParentComponent';

ReactDOM.render(
  <ParentsComponent />,
  document.getElementById('root')
);